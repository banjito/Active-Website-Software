# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Added by Claude Task Master
dev-debug.log
# Dependency directories
node_modules/
# Environment variables
.env
.vscode
# OS specific
# Task files
tasks.json
tasks/

# Electron build output — never commit (regenerated by the build; contains
# 100MB+ binaries that exceed GitHub's file-size limit).
release/
electron/renderer-dist/
# The packaged offline app is far too large for git — host it externally
# (GitHub Releases / blob storage), not in the repo.
public/assets/offline-software.zip 
# SKM PowerTools device library (120MB) + generated catalog data
PTW.LIB
*.ndjson
